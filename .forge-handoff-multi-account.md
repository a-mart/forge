## Current Task
Debugging why multi-account OpenAI usage monitoring shows both accounts as `available: false` in the sidebar widget after backend restart. The API returns data but usage fetch fails for both pooled credentials.

## Progress
- Committed multi-account usage monitoring (`c302cff`) — protocol types, backend service, frontend widget all done
- Committed copy-to-clipboard button for OAuth URL (`16fb579`)
- Committed credential pool remediation fixes (`fa6359e`) and merged to main (`686d8c7`)
- Confirmed via `curl http://127.0.0.1:47287/api/provider-usage` that both OpenAI accounts return `available: false` with correct `accountId`/`accountLabel`
- Root cause identified: `buildRuntimeAuthData()` returns `{ 'openai-codex': AuthCredential }` where `AuthCredential` is Pi's format from `~/.forge/shared/config/auth/auth.json`. But the code casts it as `CodexAuthFile` which expects `{ tokens: { access_token } }`. The Pi auth credential format is DIFFERENT from `~/.codex/auth.json` format.

## Active Files
- `apps/backend/src/stats/provider-usage-service.ts` — `refreshOpenAIIfStale()` multi-account path, ~line 130. The line `const codexAuth = authData["openai-codex"] as CodexAuthFile | undefined` is wrong — Pi's AuthCredential likely has `access_token` at top level or different structure
- `apps/backend/src/swarm/credential-pool.ts` — `buildRuntimeAuthData()` returns `Record<string, AuthCredential>`

## Next Steps
- Check what Pi's `AuthCredential` / `AuthStorage.get()` actually returns — inspect `~/.forge/shared/config/auth/auth.json` key `openai-codex` to see the shape (likely `{ access_token, refresh_token, ... }` without `tokens` wrapper)
- Fix the multi-account path in `refreshOpenAIIfStale()` to extract `access_token` from the correct location in the AuthCredential object
- Run typecheck + tests after fix

## Open Issues
- Need to determine exact shape of Pi's AuthCredential for openai-codex — may have `access_token` at top level, or nested differently than `~/.codex/auth.json`
- The single-account fallback path (reading `~/.codex/auth.json` directly) works fine — it's only the pool path that fails
- The `catch {}` blocks silently swallow errors — should add logging temporarily to see the actual error
