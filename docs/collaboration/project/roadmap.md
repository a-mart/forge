# Collaboration roadmap

## Source of truth

All current collaboration backend, UI, protocol, Docker Compose, and project-tracking work lives in `/Users/adam/repos/middleman`.

## Near-term

- Keep the main-repo collaboration server and Docker Compose path working for a primary local instance on `127.0.0.1:47387`.
- Keep the secondary local Compose instance available behind the `multi-backend-test` profile on `127.0.0.1:47388` for multi-backend UI testing.
- Keep collaboration settings, auth, channel, specialist, and skill-selection docs aligned with the code in this repo.

## Later

- Expand project-tracker entries as collaboration features move from active work into shipped behavior.
- Add links from roadmap items to implementation plans, PRs, or commits once those artifacts exist.
