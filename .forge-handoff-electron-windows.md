## Current Task
Fix macOS Electron traffic light overlap with sidebar content. The `trafficLightPosition: { x: 12, y: 12 }` was added by me in commit `38727e2` and wasn't there before — it pushes traffic lights into web content. Need to remove it to restore the original working macOS state.

## Progress
- Windows title bar: switched to native title bar with `autoHideMenuBar: true` (no frameless overlay)
- Reverted ALL UI changes (ChatHeader, AgentSidebar) — `isFramelessDesktop = false`, no drag regions, no spacers, no padding
- Fixed `setTitleBarOverlay` crash by making IPC handler a no-op
- Added `nativeTheme.themeSource = 'dark'` for dark mode default
- Title bar overlay color sync hook fixed (hex passthrough in `use-title-bar-overlay.ts`)

## Active Files
- `apps/electron/src/main.ts` — line ~510 area, the BrowserWindow config has `trafficLightPosition: { x: 12, y: 12 }` under the `darwin` branch that needs REMOVING
- `apps/ui/src/components/chat/ChatHeader.tsx` — `isFramelessDesktop = false` on line 169, all UI title bar changes already reverted
- `apps/ui/src/components/chat/AgentSidebar.tsx` — sidebar padding already reverted

## Next Steps
- Remove `trafficLightPosition: { x: 12, y: 12 }` from the macOS BrowserWindow config in `apps/electron/src/main.ts` — this was NOT in the original code before commit `38727e2` and is causing the traffic light overlap
- Commit, push, have user rebuild (`cd apps/electron && pnpm build` then `pnpm dev:electron`)
- Once macOS is confirmed working, rebuild both platform installers for updated beta release

## Open Issues
- The `nativeTheme.themeSource = 'dark'` forces dark mode at Electron level — user requested this but it also affects the native title bar color on Windows. May need to be configurable later.
- `use-title-bar-overlay.ts` hook and IPC handler still exist but are no-ops since overlay is disabled. Could clean up later.
- Current beta release v0.8.0-beta.1 on GitHub is stale — needs rebuild after all fixes are confirmed working on both platforms.
