import { CLI_EXIT_CODES, CLI_PROTOCOL_VERSION, type CliExitCode } from '@forge/protocol/cli'

export const CLI_VERSION = '0.9.0'
export { CLI_PROTOCOL_VERSION }
export const EXIT_CODES = CLI_EXIT_CODES
export type ExitCode = CliExitCode
