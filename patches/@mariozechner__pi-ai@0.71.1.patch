diff --git a/dist/providers/openai-codex-responses.js b/dist/providers/openai-codex-responses.js
index 4c74f00dc9ca24f1528f04a67be5ca45a4c01093..fea946a7f4d2c63f95a9fb60d03b07d321dba6ba 100644
--- a/dist/providers/openai-codex-responses.js
+++ b/dist/providers/openai-codex-responses.js
@@ -90,12 +90,14 @@ export const streamOpenAICodexResponses = (model, context, options) => {
             const websocketHeaders = buildWebSocketHeaders(model.headers, options?.headers, accountId, apiKey, websocketRequestId);
             const bodyJson = JSON.stringify(body);
             const transport = options?.transport || "sse";
+            let streamStarted = false;
+            const initialOutputState = cloneOutputState(output);
             if (transport !== "sse") {
-                let websocketStarted = false;
-                try {
-                    await processWebSocketStream(resolveCodexWebSocketUrl(model.baseUrl), body, websocketHeaders, output, stream, model, () => {
-                        websocketStarted = true;
-                    }, options);
+                const markWebSocketStarted = () => {
+                    streamStarted = true;
+                };
+                const runWebSocketAttempt = async ({ suppressStart = false, forceFresh = false, forceFullBody = false } = {}) => {
+                    await processWebSocketStream(resolveCodexWebSocketUrl(model.baseUrl), body, websocketHeaders, output, stream, model, markWebSocketStarted, options, { suppressStart, forceFresh, forceFullBody });
                     if (options?.signal?.aborted) {
                         throw new Error("Request was aborted");
                     }
@@ -105,10 +107,33 @@ export const streamOpenAICodexResponses = (model, context, options) => {
                         message: output,
                     });
                     stream.end();
+                };
+                try {
+                    await runWebSocketAttempt();
                     return;
                 }
                 catch (error) {
-                    if (transport === "websocket" || transport === "websocket-cached" || websocketStarted) {
+                    const canReplaySafely = canReplayWebSocketFailure(error, output, options);
+                    if (canReplaySafely) {
+                        resetOutputState(output, initialOutputState);
+                    }
+                    if (canReplaySafely && (transport === "websocket" || transport === "websocket-cached")) {
+                        try {
+                            await runWebSocketAttempt({ suppressStart: streamStarted, forceFresh: true, forceFullBody: true });
+                            return;
+                        }
+                        catch (retryError) {
+                            const canFallbackSafely = transport === "websocket-cached" && canReplayWebSocketFailure(retryError, output, options);
+                            if (!canFallbackSafely) {
+                                throw retryError;
+                            }
+                            resetOutputState(output, initialOutputState);
+                        }
+                    }
+                    else if (transport === "auto" && !streamStarted) {
+                        resetOutputState(output, initialOutputState);
+                    }
+                    else if (transport === "websocket" || transport === "websocket-cached" || !canReplaySafely) {
                         throw error;
                     }
                 }
@@ -167,7 +192,9 @@ export const streamOpenAICodexResponses = (model, context, options) => {
             if (!response.body) {
                 throw new Error("No response body");
             }
-            stream.push({ type: "start", partial: output });
+            if (!streamStarted) {
+                stream.push({ type: "start", partial: output });
+            }
             await processStream(response, output, stream, model, options);
             if (options?.signal?.aborted) {
                 throw new Error("Request was aborted");
@@ -495,7 +522,7 @@ async function connectWebSocket(url, headers, signal) {
             reject(error);
         };
         const onClose = (event) => {
-            const error = extractWebSocketCloseError(event);
+            const error = extractWebSocketCloseError(event, "connect");
             if (settled)
                 return;
             settled = true;
@@ -522,7 +549,16 @@ async function connectWebSocket(url, headers, signal) {
         signal?.addEventListener("abort", onAbort);
     });
 }
-async function acquireWebSocket(url, headers, sessionId, signal) {
+async function acquireWebSocket(url, headers, sessionId, signal, forceFresh = false) {
+    if (forceFresh && sessionId) {
+        const cached = websocketSessionCache.get(sessionId);
+        if (cached) {
+            closeWebSocketSilently(cached.socket);
+            if (cached.idleTimer)
+                clearTimeout(cached.idleTimer);
+            websocketSessionCache.delete(sessionId);
+        }
+    }
     if (!sessionId) {
         const socket = await connectWebSocket(url, headers, signal);
         return {
@@ -606,15 +642,65 @@ function extractWebSocketError(event) {
     }
     return new Error("WebSocket error");
 }
-function extractWebSocketCloseError(event) {
+function createWebSocketCloseError(message, details = {}) {
+    const error = new Error(message);
+    error.name = "WebSocketCloseError";
+    if (details.code !== undefined)
+        error.websocketCloseCode = details.code;
+    if (details.reason !== undefined)
+        error.websocketCloseReason = details.reason;
+    if (details.phase !== undefined)
+        error.websocketPhase = details.phase;
+    if (details.beforeCompletion !== undefined)
+        error.websocketClosedBeforeCompletion = details.beforeCompletion;
+    return error;
+}
+function extractWebSocketCloseError(event, phase) {
     if (event && typeof event === "object") {
         const code = "code" in event ? event.code : undefined;
         const reason = "reason" in event ? event.reason : undefined;
         const codeText = typeof code === "number" ? ` ${code}` : "";
         const reasonText = typeof reason === "string" && reason.length > 0 ? ` ${reason}` : "";
-        return new Error(`WebSocket closed${codeText}${reasonText}`.trim());
+        const phaseText = phase ? ` (phase: ${phase})` : "";
+        return createWebSocketCloseError(`WebSocket closed${codeText}${reasonText}${phaseText}`.trim(), {
+            code,
+            reason,
+            phase,
+            beforeCompletion: phase !== "connect",
+        });
+    }
+    return createWebSocketCloseError(`WebSocket closed${phase ? ` (phase: ${phase})` : ""}`, { phase, beforeCompletion: phase !== "connect" });
+}
+function isAbortError(error) {
+    return error instanceof Error && (error.name === "AbortError" || error.message === "Request was aborted");
+}
+function isRetryableWebSocketCloseBeforeOutput(error) {
+    if (!(error instanceof Error) || isAbortError(error))
+        return false;
+    const closeCode = error.websocketCloseCode;
+    if (closeCode === 1006)
+        return true;
+    return error.name === "WebSocketCloseError" && (error.websocketClosedBeforeCompletion === true || /WebSocket stream closed before response\.completed/i.test(error.message));
+}
+function canReplayWebSocketFailure(error, output, options) {
+    return !options?.signal?.aborted && output.content.length === 0 && isRetryableWebSocketCloseBeforeOutput(error);
+}
+function cloneOutputState(output) {
+    return {
+        ...output,
+        content: output.content.map((block) => ({ ...block })),
+        usage: { ...output.usage, cost: { ...output.usage.cost } },
+    };
+}
+function resetOutputState(output, state) {
+    for (const key of Object.keys(output)) {
+        delete output[key];
     }
-    return new Error("WebSocket closed");
+    Object.assign(output, {
+        ...state,
+        content: state.content.map((block) => ({ ...block })),
+        usage: { ...state.usage, cost: { ...state.usage.cost } },
+    });
 }
 async function decodeWebSocketData(data) {
     if (typeof data === "string")
@@ -639,6 +725,7 @@ async function* parseWebSocket(socket, signal) {
     let done = false;
     let failed = null;
     let sawCompletion = false;
+    let phase = "awaiting_message";
     const wake = () => {
         if (!pending)
             return;
@@ -646,22 +733,46 @@ async function* parseWebSocket(socket, signal) {
         pending = null;
         resolve();
     };
+    const enqueueParsedMessage = (parsed) => {
+        const type = typeof parsed.type === "string" ? parsed.type : "";
+        if (type === "response.created") {
+            phase = "response_created";
+        }
+        else if (type === "response.output_item.added") {
+            const itemType = typeof parsed.item?.type === "string" ? parsed.item.type : "unknown";
+            phase = `output_item_added:${itemType}`;
+        }
+        else if (type === "response.output_text.delta" || type === "response.function_call_arguments.delta" || type === "response.reasoning_summary_text.delta") {
+            phase = type;
+        }
+        else if (type === "response.output_item.done") {
+            const itemType = typeof parsed.item?.type === "string" ? parsed.item.type : "unknown";
+            phase = `output_item_done:${itemType}`;
+        }
+        else if (type === "response.completed" || type === "response.done" || type === "response.incomplete") {
+            phase = type;
+            sawCompletion = true;
+            done = true;
+        }
+        queue.push(parsed);
+        wake();
+    };
     const onMessage = (event) => {
+        if (!event || typeof event !== "object" || !("data" in event))
+            return;
+        if (typeof event.data === "string") {
+            try {
+                enqueueParsedMessage(JSON.parse(event.data));
+            }
+            catch { }
+            return;
+        }
         void (async () => {
-            if (!event || typeof event !== "object" || !("data" in event))
-                return;
             const text = await decodeWebSocketData(event.data);
             if (!text)
                 return;
             try {
-                const parsed = JSON.parse(text);
-                const type = typeof parsed.type === "string" ? parsed.type : "";
-                if (type === "response.completed" || type === "response.done" || type === "response.incomplete") {
-                    sawCompletion = true;
-                    done = true;
-                }
-                queue.push(parsed);
-                wake();
+                enqueueParsedMessage(JSON.parse(text));
             }
             catch { }
         })();
@@ -678,7 +789,7 @@ async function* parseWebSocket(socket, signal) {
             return;
         }
         if (!failed) {
-            failed = extractWebSocketCloseError(event);
+            failed = extractWebSocketCloseError(event, phase);
         }
         done = true;
         wake();
@@ -711,7 +822,7 @@ async function* parseWebSocket(socket, signal) {
             throw failed;
         }
         if (!sawCompletion) {
-            throw new Error("WebSocket stream closed before response.completed");
+            throw createWebSocketCloseError(`WebSocket stream closed before response.completed (phase: ${phase})`, { phase, beforeCompletion: true });
         }
     }
     finally {
@@ -762,14 +873,15 @@ function buildCachedWebSocketRequestBody(entry, body) {
         input: delta,
     };
 }
-async function processWebSocketStream(url, body, headers, output, stream, model, onStart, options) {
-    const { socket, entry, reused, release } = await acquireWebSocket(url, headers, options?.sessionId, options?.signal);
+async function processWebSocketStream(url, body, headers, output, stream, model, onStart, options, attemptOptions = {}) {
+    const { suppressStart = false, forceFresh = false, forceFullBody = false } = attemptOptions;
+    const { socket, entry, reused, release } = await acquireWebSocket(url, headers, options?.sessionId, options?.signal, forceFresh);
     let keepConnection = true;
     const useCachedContext = options?.transport === "websocket-cached";
     // ChatGPT Codex Responses rejects `store: true` ("Store must be set to false").
     // WebSocket continuation still works via connection-scoped previous_response_id state.
     const fullBody = body;
-    const requestBody = useCachedContext && entry ? buildCachedWebSocketRequestBody(entry, fullBody) : fullBody;
+    const requestBody = useCachedContext && entry && !forceFullBody ? buildCachedWebSocketRequestBody(entry, fullBody) : fullBody;
     const stats = options?.sessionId ? getOrCreateWebSocketDebugStats(options.sessionId) : undefined;
     if (stats) {
         stats.requests++;
@@ -796,7 +908,9 @@ async function processWebSocketStream(url, body, headers, output, stream, model,
     try {
         socket.send(JSON.stringify({ type: "response.create", ...requestBody }));
         onStart();
-        stream.push({ type: "start", partial: output });
+        if (!suppressStart) {
+            stream.push({ type: "start", partial: output });
+        }
         await processResponsesStream(mapCodexEvents(parseWebSocket(socket, options?.signal)), output, stream, model, {
             serviceTier: options?.serviceTier,
             resolveServiceTier: resolveCodexServiceTier,
diff --git a/dist/providers/openai-completions.js b/dist/providers/openai-completions.js
index 34ba15afe67da4c5e3d3d7ecafc77814ba5e5cef..85bb1fe52c7932d110ce520a3256680e28945ff8 100644
--- a/dist/providers/openai-completions.js
+++ b/dist/providers/openai-completions.js
@@ -680,6 +680,15 @@ export function convertMessages(model, context, compat) {
                 assistantMsg.reasoning_content === undefined) {
                 assistantMsg.reasoning_content = "";
             }
+            // xAI rejects content:null/"" on tool-call messages; OpenAI/Anthropic/Google accept
+            // it (pi's upstream default is null and works for them). Scope the placeholder to xAI
+            // so other providers keep clean null content instead of a synthetic blank assistant
+            // text item, which would otherwise seed the same empty-turn imitation loop on replay.
+            const isXaiCompletionsModel = model.provider === "xai" || String(model.baseUrl ?? "").includes("api.x.ai");
+            if (isXaiCompletionsModel && assistantMsg.tool_calls && assistantMsg.tool_calls.length > 0 &&
+                (assistantMsg.content === null || assistantMsg.content === undefined || assistantMsg.content === "")) {
+                assistantMsg.content = " ";
+            }
             // Skip assistant messages that have no content and no tool calls.
             // Some providers require "either content or tool_calls, but not none".
             // Other providers also don't accept empty assistant messages.
diff --git a/dist/providers/openai-responses-shared.js b/dist/providers/openai-responses-shared.js
index 3081455a347db90995fd51f654d0ec48f16cc73d..c3ea50147ddbcd992dbef7fe6ecb8fd63391d21d 100644
--- a/dist/providers/openai-responses-shared.js
+++ b/dist/providers/openai-responses-shared.js
@@ -103,18 +103,47 @@ export function convertResponsesMessages(model, context, allowedToolCallProvider
         else if (msg.role === "assistant") {
             const output = [];
             const assistantMsg = msg;
+            // Silent turns (no tool calls, no non-whitespace text) carry zero
+            // information, and replaying them teaches the model that empty
+            // responses are its own normal behavior — a self-reinforcing loop
+            // measured at up to 100% empty-turn probability in contaminated
+            // sessions. Drop the whole message (including its reasoning items)
+            // from replay; the session file keeps the historical record.
+            const isSilentAssistantTurn = !msg.content.some((block) => block.type === "toolCall" ||
+                (block.type === "text" && typeof block.text === "string" && block.text.trim().length > 0));
+            if (isSilentAssistantTurn) {
+                continue;
+            }
             const isDifferentModel = assistantMsg.model !== model.id &&
                 assistantMsg.provider === model.provider &&
                 assistantMsg.api === model.api;
+            let hasAssistantMessage = false;
+            let hasToolCalls = false;
+            const isXaiResponsesModel = model.provider === "xai" || String(model.baseUrl ?? "").includes("api.x.ai");
+            const shouldReplayHistoricalReasoning = !isXaiResponsesModel;
             for (const block of msg.content) {
                 if (block.type === "thinking") {
-                    if (block.thinkingSignature) {
+                    // xAI's Responses-compatible endpoint rejects historical reasoning items even
+                    // when the rest of the assistant/tool-call turn is valid. Keep replaying them
+                    // for providers that accept OpenAI Responses reasoning history.
+                    if (block.thinkingSignature && shouldReplayHistoricalReasoning) {
                         const reasoningItem = JSON.parse(block.thinkingSignature);
                         output.push(reasoningItem);
                     }
                 }
                 else if (block.type === "text") {
                     const textBlock = block;
+                    const assistantText = sanitizeSurrogates(textBlock.text);
+                    // Never replay an empty assistant text item to any Responses model.
+                    // An empty output_text carries no information and trains the model that
+                    // empty turns are its own normal behavior — a self-reinforcing loop that
+                    // drives sessions toward silence (gpt-5.x is especially prone). Codex
+                    // commonly emits an empty "commentary" text block alongside tool calls;
+                    // skipping it here keeps that contamination out of replay. Wholly-silent
+                    // turns (no tool calls either) are dropped above via isSilentAssistantTurn.
+                    if (assistantText.trim().length === 0) {
+                        continue;
+                    }
                     const parsedSignature = parseTextSignature(textBlock.textSignature);
                     // OpenAI requires id to be max 64 characters
                     let msgId = parsedSignature?.id;
@@ -127,11 +156,12 @@ export function convertResponsesMessages(model, context, allowedToolCallProvider
                     output.push({
                         type: "message",
                         role: "assistant",
-                        content: [{ type: "output_text", text: sanitizeSurrogates(textBlock.text), annotations: [] }],
+                        content: [{ type: "output_text", text: assistantText, annotations: [] }],
                         status: "completed",
                         id: msgId,
                         phase: parsedSignature?.phase,
                     });
+                    hasAssistantMessage = true;
                 }
                 else if (block.type === "toolCall") {
                     const toolCall = block;
@@ -150,8 +180,23 @@ export function convertResponsesMessages(model, context, allowedToolCallProvider
                         name: toolCall.name,
                         arguments: JSON.stringify(toolCall.arguments),
                     });
+                    hasToolCalls = true;
                 }
             }
+            // xAI's Responses-compatible endpoint rejects assistant/tool-call turns that omit
+            // the assistant message item entirely. Mirror the chat-completions fix by inserting
+            // a non-empty placeholder assistant message before the function_call items when the
+            // turn has tools but no text. xAI-scoped: Codex accepts tool-only turns natively.
+            if (isXaiResponsesModel && hasToolCalls && !hasAssistantMessage) {
+                const firstFunctionCallIndex = output.findIndex((item) => item.type === "function_call");
+                output.splice(firstFunctionCallIndex >= 0 ? firstFunctionCallIndex : output.length, 0, {
+                    type: "message",
+                    role: "assistant",
+                    content: [{ type: "output_text", text: " ", annotations: [] }],
+                    status: "completed",
+                    id: `msg_${msgIndex}`,
+                });
+            }
             if (output.length === 0)
                 continue;
             messages.push(...output);
diff --git a/dist/providers/simple-options.js b/dist/providers/simple-options.js
index d69e2b6bf47bf2d1dd176eed832f75612bc8ee5a..64c0447b9d0c33454d13bc6697ba67e815721e93 100644
--- a/dist/providers/simple-options.js
+++ b/dist/providers/simple-options.js
@@ -4,6 +4,7 @@ export function buildBaseOptions(model, options, apiKey) {
         maxTokens: options?.maxTokens ?? (model.maxTokens > 0 ? Math.min(model.maxTokens, 32000) : undefined),
         signal: options?.signal,
         apiKey: apiKey || options?.apiKey,
+        transport: options?.transport,
         cacheRetention: options?.cacheRetention,
         sessionId: options?.sessionId,
         headers: options?.headers,
