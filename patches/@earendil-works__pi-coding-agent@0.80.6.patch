diff --git a/dist/core/agent-session.d.ts b/dist/core/agent-session.d.ts
index 3d3a9e90efa1c1905fc4c40306f7b93fb7f8d92f..3e9fa6f63f6c51a03b0c7954bb3834a37ecb671e 100644
--- a/dist/core/agent-session.d.ts
+++ b/dist/core/agent-session.d.ts
@@ -184,6 +184,8 @@ export declare class AgentSession {
     private _compactionAbortController;
     private _autoCompactionAbortController;
     private _overflowRecoveryAttempted;
+    private _freshContextHandler?;
+    private _freshContextBoundaryHandler?;
     private _branchSummaryAbortController;
     private _retryAbortController;
     private _retryAttempt;
@@ -467,6 +469,22 @@ export declare class AgentSession {
      * Saves to settings.
      */
     setFollowUpMode(mode: "all" | "one-at-a-time"): void;
+    /**
+     * Optional Forge fresh-window handler. Called at existing manual/automatic
+     * compaction boundaries before auth/preparation. Return undefined to keep the
+     * existing summary path unchanged. Handler errors fail closed.
+     */
+    setFreshContextHandler(handler?: (request: {
+        reason: "manual" | "threshold" | "overflow" | "agent";
+        willRetry: boolean;
+        branchEntries: SessionEntry[];
+        tokensBefore?: number;
+        signal?: AbortSignal;
+    }) => Promise<{
+        summary: string;
+        tokensBefore: number;
+        details?: unknown;
+    } | undefined>): void;
     /**
      * Manually compact the session context.
      * Aborts current agent operation first.
@@ -483,6 +501,12 @@ export declare class AgentSession {
     abortBranchSummary(): void;
     private _checkCompaction;
     private _runAutoCompaction;
+    /** Called after the complete tool batch is durable, before the next model request. */
+    setFreshContextBoundaryHandler(handler?: (signal?: AbortSignal) => Promise<"agent" | "threshold" | undefined>): void;
+    private _resolveFreshContextResult;
+    private _materializeFreshContext;
+    private _commitCompaction;
+    private _notifyCompactionCommitted;
     /**
      * Toggle auto-compaction setting.
      */
diff --git a/dist/core/agent-session.js b/dist/core/agent-session.js
index a208774c956e3039c47ca54ce1f2f362ce3e4db3..75e9a49a669ca5640608eb8f63368164e90d066f 100644
--- a/dist/core/agent-session.js
+++ b/dist/core/agent-session.js
@@ -85,6 +85,8 @@ export class AgentSession {
     _compactionAbortController = undefined;
     _autoCompactionAbortController = undefined;
     _overflowRecoveryAttempted = false;
+    _freshContextHandler = undefined;
+    _freshContextBoundaryHandler = undefined;
     // Branch summarization state
     _branchSummaryAbortController = undefined;
     // Retry state
@@ -240,10 +242,27 @@ export class AgentSession {
         this.agent.prepareNextTurnWithContext = async (turn, signal) => {
             const previousSnapshot = await previousPrepareNextTurnWithContext?.(turn, signal);
             const previousContext = previousSnapshot?.context ?? turn.context;
+            // Agent-core calls this only after every tool result and turn_end have settled.
+            // Replace the loop snapshot as well as Agent.state, so the next model request
+            // cannot accidentally resurrect the pre-boundary messages.
+            let freshCommitted = false;
+            if (turn.toolResults.length > 0 && !signal?.aborted) {
+                const reason = await this._freshContextBoundaryHandler?.(signal);
+                if (reason && !signal?.aborted) {
+                    freshCommitted = await this._runAutoCompaction("threshold", true, reason, signal);
+                    if (!freshCommitted && !signal?.aborted) {
+                        await this.sendCustomMessage({
+                            customType: "forge_context_transition_failed", display: false,
+                            content: "The requested fresh context was not committed. Your previous context is intact. Incorporate any new input, verify or repair notes checkpoint.md, and continue the task. Do not repeat completed side effects. You can request new_context again when ready.",
+                        }, { deliverAs: "steer" });
+                    }
+                }
+            }
             return {
                 ...previousSnapshot,
                 context: {
                     ...previousContext,
+                    ...(freshCommitted ? { messages: this.agent.state.messages.slice() } : {}),
                     systemPrompt: this._systemPromptOverride ?? this._baseSystemPrompt,
                     tools: this.agent.state.tools.slice(),
                 },
@@ -1334,6 +1353,18 @@ export class AgentSession {
         this.agent.followUpMode = mode;
         this.settingsManager.setFollowUpMode(mode);
     }
+    /**
+     * Optional Forge fresh-window handler. Called at existing manual/automatic
+     * compaction boundaries before auth/preparation. Return undefined to keep the
+     * existing summary path unchanged. Handler errors fail closed.
+     */
+    setFreshContextHandler(handler) {
+        this._freshContextHandler = handler;
+    }
+    /** Forge policy at the settled tool boundary; never called from inside a tool. */
+    setFreshContextBoundaryHandler(handler) {
+        this._freshContextBoundaryHandler = handler;
+    }
     // =========================================================================
     // Compaction
     // =========================================================================
@@ -1343,89 +1374,105 @@ export class AgentSession {
      * @param customInstructions Optional instructions for the compaction summary
      */
     async compact(customInstructions) {
-        this._disconnectFromAgent();
-        await this.abort();
+        if (this._compactionAbortController || this._autoCompactionAbortController) {
+            throw new Error("Compaction already in progress");
+        }
         this._compactionAbortController = new AbortController();
+        this._disconnectFromAgent();
+        try {
+            await this.abort();
+        }
+        catch (error) {
+            this._compactionAbortController = undefined;
+            this._reconnectToAgent();
+            throw error;
+        }
         this._emit({ type: "compaction_start", reason: "manual" });
         try {
-            if (!this.model) {
-                throw new Error(formatNoModelSelectedMessage());
-            }
-            const { apiKey, headers, env } = await this._getCompactionRequestAuth(this.model);
             const pathEntries = this.sessionManager.getBranch();
-            const settings = this.settingsManager.getCompactionSettings();
-            const preparation = prepareCompaction(pathEntries, settings);
-            if (!preparation) {
-                // Check why we can't compact
-                const lastEntry = pathEntries[pathEntries.length - 1];
-                if (lastEntry?.type === "compaction") {
-                    throw new Error("Already compacted");
-                }
-                throw new Error("Nothing to compact (session too small)");
-            }
-            let extensionCompaction;
-            let fromExtension = false;
-            if (this._extensionRunner.hasHandlers("session_before_compact")) {
-                const result = (await this._extensionRunner.emit({
-                    type: "session_before_compact",
-                    preparation,
-                    branchEntries: pathEntries,
-                    customInstructions,
-                    reason: "manual",
-                    willRetry: false,
-                    signal: this._compactionAbortController.signal,
-                }));
-                if (result?.cancel) {
-                    throw new Error("Compaction cancelled");
-                }
-                if (result?.compaction) {
-                    extensionCompaction = result.compaction;
-                    fromExtension = true;
-                }
+            const freshResult = await this._resolveFreshContextResult({
+                reason: "manual",
+                willRetry: false,
+                branchEntries: pathEntries,
+                tokensBefore: estimateMessagesTokens(this.agent.state.messages),
+                signal: this._compactionAbortController.signal,
+            });
+            if (this._compactionAbortController.signal.aborted) {
+                throw new Error("Compaction cancelled");
             }
             let summary;
             let firstKeptEntryId;
             let tokensBefore;
             let details;
-            if (extensionCompaction) {
-                // Extension provided compaction content
-                summary = extensionCompaction.summary;
-                firstKeptEntryId = extensionCompaction.firstKeptEntryId;
-                tokensBefore = extensionCompaction.tokensBefore;
-                details = extensionCompaction.details;
+            let fromExtension = false;
+            if (freshResult) {
+                ({ summary, firstKeptEntryId, tokensBefore, details } = this._materializeFreshContext(freshResult));
             }
             else {
-                // Generate compaction result
-                const result = await compact(preparation, this.model, apiKey, headers, customInstructions, this._compactionAbortController.signal, this.thinkingLevel, this.agent.streamFn, env);
-                summary = result.summary;
-                firstKeptEntryId = result.firstKeptEntryId;
-                tokensBefore = result.tokensBefore;
-                details = result.details;
+                if (!this.model) {
+                    throw new Error(formatNoModelSelectedMessage());
+                }
+                const { apiKey, headers, env } = await this._getCompactionRequestAuth(this.model);
+                const settings = this.settingsManager.getCompactionSettings();
+                const preparation = prepareCompaction(pathEntries, settings);
+                if (!preparation) {
+                    // Check why we can't compact
+                    const lastEntry = pathEntries[pathEntries.length - 1];
+                    if (lastEntry?.type === "compaction") {
+                        throw new Error("Already compacted");
+                    }
+                    throw new Error("Nothing to compact (session too small)");
+                }
+                let extensionCompaction;
+                if (this._extensionRunner.hasHandlers("session_before_compact")) {
+                    const result = (await this._extensionRunner.emit({
+                        type: "session_before_compact",
+                        preparation,
+                        branchEntries: pathEntries,
+                        customInstructions,
+                        reason: "manual",
+                        willRetry: false,
+                        signal: this._compactionAbortController.signal,
+                    }));
+                    if (result?.cancel) {
+                        throw new Error("Compaction cancelled");
+                    }
+                    if (result?.compaction) {
+                        extensionCompaction = result.compaction;
+                        fromExtension = true;
+                    }
+                }
+                if (extensionCompaction) {
+                    // Extension provided compaction content
+                    summary = extensionCompaction.summary;
+                    firstKeptEntryId = extensionCompaction.firstKeptEntryId;
+                    tokensBefore = extensionCompaction.tokensBefore;
+                    details = extensionCompaction.details;
+                }
+                else {
+                    // Generate compaction result
+                    const result = await compact(preparation, this.model, apiKey, headers, customInstructions, this._compactionAbortController.signal, this.thinkingLevel, this.agent.streamFn, env);
+                    summary = result.summary;
+                    firstKeptEntryId = result.firstKeptEntryId;
+                    tokensBefore = result.tokensBefore;
+                    details = result.details;
+                }
             }
             if (this._compactionAbortController.signal.aborted) {
                 throw new Error("Compaction cancelled");
             }
-            this.sessionManager.appendCompaction(summary, firstKeptEntryId, tokensBefore, details, fromExtension);
-            const newEntries = this.sessionManager.getEntries();
-            const sessionContext = this.sessionManager.buildSessionContext();
-            this.agent.state.messages = sessionContext.messages;
-            const estimatedTokensAfter = estimateMessagesTokens(sessionContext.messages);
-            // Get the saved compaction entry for the extension event
-            const savedCompactionEntry = newEntries.find((e) => e.type === "compaction" && e.summary === summary);
-            if (this._extensionRunner && savedCompactionEntry) {
-                await this._extensionRunner.emit({
-                    type: "session_compact",
-                    compactionEntry: savedCompactionEntry,
-                    fromExtension,
-                    reason: "manual",
-                    willRetry: false,
-                });
-            }
+            const committed = this._commitCompaction(summary, firstKeptEntryId, tokensBefore, details, fromExtension);
+            await this._notifyCompactionCommitted({
+                savedCompactionEntry: committed.savedCompactionEntry,
+                fromExtension,
+                reason: "manual",
+                willRetry: false,
+            });
             const compactionResult = {
                 summary,
                 firstKeptEntryId,
                 tokensBefore,
-                estimatedTokensAfter,
+                estimatedTokensAfter: committed.estimatedTokensAfter,
                 details,
             };
             this._emit({
@@ -1562,83 +1609,126 @@ export class AgentSession {
     /**
      * Internal: Run auto-compaction with events.
      */
-    async _runAutoCompaction(reason, willRetry) {
+    async _runAutoCompaction(reason, willRetry, freshReason, parentSignal) {
+        // Reentrancy guard: if compaction is already in progress, bail out.
+        // This prevents a race where two concurrent calls (from agent_end handler
+        // and prompt's pre-check) corrupt the shared _autoCompactionAbortController.
+        if (this._autoCompactionAbortController || this._compactionAbortController) {
+            return false;
+        }
+        let localAbortController = new AbortController();
+        this._autoCompactionAbortController = localAbortController;
+        const abortFromParent = () => localAbortController.abort();
+        parentSignal?.addEventListener("abort", abortFromParent, { once: true });
+        if (parentSignal?.aborted) localAbortController.abort();
         const settings = this.settingsManager.getCompactionSettings();
         let started = false;
         try {
-            if (!this.model) {
-                return false;
-            }
-            let apiKey;
-            let headers;
-            let env;
-            if (this.agent.streamFn === streamSimple) {
-                const authResult = await this._modelRegistry.getApiKeyAndHeaders(this.model);
-                if (!authResult.ok || !authResult.apiKey) {
-                    return false;
-                }
-                apiKey = authResult.apiKey;
-                headers = authResult.headers;
-                env = authResult.env;
-            }
-            else {
-                ({ apiKey, headers, env } = await this._getCompactionRequestAuth(this.model));
-            }
             const pathEntries = this.sessionManager.getBranch();
-            const preparation = prepareCompaction(pathEntries, settings);
-            if (!preparation) {
-                return false;
-            }
-            this._emit({ type: "compaction_start", reason });
-            this._autoCompactionAbortController = new AbortController();
-            started = true;
-            let extensionCompaction;
-            let fromExtension = false;
-            if (this._extensionRunner.hasHandlers("session_before_compact")) {
-                const extensionResult = (await this._extensionRunner.emit({
-                    type: "session_before_compact",
-                    preparation,
-                    branchEntries: pathEntries,
-                    customInstructions: undefined,
-                    reason,
-                    willRetry,
-                    signal: this._autoCompactionAbortController.signal,
-                }));
-                if (extensionResult?.cancel) {
-                    this._emit({
-                        type: "compaction_end",
-                        reason,
-                        result: undefined,
-                        aborted: true,
-                        willRetry: false,
-                    });
-                    return false;
-                }
-                if (extensionResult?.compaction) {
-                    extensionCompaction = extensionResult.compaction;
-                    fromExtension = true;
+            const freshResult = await this._resolveFreshContextResult({
+                reason: freshReason ?? reason,
+                willRetry,
+                branchEntries: pathEntries,
+                tokensBefore: estimateMessagesTokens(this.agent.state.messages),
+                signal: localAbortController.signal,
+            });
+            if (localAbortController.signal.aborted) {
+                if (!started) {
+                    this._emit({ type: "compaction_start", reason });
+                    started = true;
                 }
+                this._emit({
+                    type: "compaction_end",
+                    reason,
+                    result: undefined,
+                    aborted: true,
+                    willRetry: false,
+                });
+                return false;
             }
+            // A boundary request is Fresh-only. A changed policy must not silently
+            // invoke a summarizer in the middle of the active agent loop.
+            if (freshReason && !freshResult) return false;
             let summary;
             let firstKeptEntryId;
             let tokensBefore;
             let details;
-            if (extensionCompaction) {
-                // Extension provided compaction content
-                summary = extensionCompaction.summary;
-                firstKeptEntryId = extensionCompaction.firstKeptEntryId;
-                tokensBefore = extensionCompaction.tokensBefore;
-                details = extensionCompaction.details;
+            let fromExtension = false;
+            if (freshResult) {
+                if (!started) {
+                    this._emit({ type: "compaction_start", reason });
+                    started = true;
+                }
+                ({ summary, firstKeptEntryId, tokensBefore, details } = this._materializeFreshContext(freshResult));
             }
             else {
-                // Generate compaction result
-                const compactResult = await compact(preparation, this.model, apiKey, headers, undefined, this._autoCompactionAbortController.signal, this.thinkingLevel, this.agent.streamFn, env);
-                summary = compactResult.summary;
-                firstKeptEntryId = compactResult.firstKeptEntryId;
-                tokensBefore = compactResult.tokensBefore;
-                details = compactResult.details;
-            }
-            if (this._autoCompactionAbortController.signal.aborted) {
+                if (!this.model) {
+                    return false;
+                }
+                let apiKey;
+                let headers;
+                let env;
+                if (this.agent.streamFn === streamSimple) {
+                    const authResult = await this._modelRegistry.getApiKeyAndHeaders(this.model);
+                    if (!authResult.ok || !authResult.apiKey) {
+                        return false;
+                    }
+                    apiKey = authResult.apiKey;
+                    headers = authResult.headers;
+                    env = authResult.env;
+                }
+                else {
+                    ({ apiKey, headers, env } = await this._getCompactionRequestAuth(this.model));
+                }
+                const preparation = prepareCompaction(pathEntries, settings);
+                if (!preparation) {
+                    return false;
+                }
+                this._emit({ type: "compaction_start", reason });
+                started = true;
+                let extensionCompaction;
+                if (this._extensionRunner.hasHandlers("session_before_compact")) {
+                    const extensionResult = (await this._extensionRunner.emit({
+                        type: "session_before_compact",
+                        preparation,
+                        branchEntries: pathEntries,
+                        customInstructions: undefined,
+                        reason,
+                        willRetry,
+                        signal: localAbortController.signal,
+                    }));
+                    if (extensionResult?.cancel) {
+                        this._emit({
+                            type: "compaction_end",
+                            reason,
+                            result: undefined,
+                            aborted: true,
+                            willRetry: false,
+                        });
+                        return false;
+                    }
+                    if (extensionResult?.compaction) {
+                        extensionCompaction = extensionResult.compaction;
+                        fromExtension = true;
+                    }
+                }
+                if (extensionCompaction) {
+                    // Extension provided compaction content
+                    summary = extensionCompaction.summary;
+                    firstKeptEntryId = extensionCompaction.firstKeptEntryId;
+                    tokensBefore = extensionCompaction.tokensBefore;
+                    details = extensionCompaction.details;
+                }
+                else {
+                    // Generate compaction result
+                    const compactResult = await compact(preparation, this.model, apiKey, headers, undefined, localAbortController.signal, this.thinkingLevel, this.agent.streamFn, env);
+                    summary = compactResult.summary;
+                    firstKeptEntryId = compactResult.firstKeptEntryId;
+                    tokensBefore = compactResult.tokensBefore;
+                    details = compactResult.details;
+                }
+            }
+            if (localAbortController.signal.aborted) {
                 this._emit({
                     type: "compaction_end",
                     reason,
@@ -1648,27 +1738,18 @@ export class AgentSession {
                 });
                 return false;
             }
-            this.sessionManager.appendCompaction(summary, firstKeptEntryId, tokensBefore, details, fromExtension);
-            const newEntries = this.sessionManager.getEntries();
-            const sessionContext = this.sessionManager.buildSessionContext();
-            this.agent.state.messages = sessionContext.messages;
-            const estimatedTokensAfter = estimateMessagesTokens(sessionContext.messages);
-            // Get the saved compaction entry for the extension event
-            const savedCompactionEntry = newEntries.find((e) => e.type === "compaction" && e.summary === summary);
-            if (this._extensionRunner && savedCompactionEntry) {
-                await this._extensionRunner.emit({
-                    type: "session_compact",
-                    compactionEntry: savedCompactionEntry,
-                    fromExtension,
-                    reason,
-                    willRetry,
-                });
-            }
+            const committed = this._commitCompaction(summary, firstKeptEntryId, tokensBefore, details, fromExtension);
+            await this._notifyCompactionCommitted({
+                savedCompactionEntry: committed.savedCompactionEntry,
+                fromExtension,
+                reason,
+                willRetry,
+            });
             const result = {
                 summary,
                 firstKeptEntryId,
                 tokensBefore,
-                estimatedTokensAfter,
+                estimatedTokensAfter: committed.estimatedTokensAfter,
                 details,
             };
             this._emit({ type: "compaction_end", reason, result, aborted: false, willRetry });
@@ -1686,22 +1767,89 @@ export class AgentSession {
         }
         catch (error) {
             const errorMessage = error instanceof Error ? error.message : "compaction failed";
+            const aborted = error instanceof Error && (error.name === "AbortError" || error.message === "Compaction cancelled");
+            const shouldReport = started || aborted || typeof this._freshContextHandler === "function";
+            if (shouldReport && !started) {
+                this._emit({ type: "compaction_start", reason });
+                started = true;
+            }
             if (started) {
                 this._emit({
                     type: "compaction_end",
                     reason,
                     result: undefined,
-                    aborted: false,
+                    aborted,
                     willRetry: false,
-                    errorMessage: reason === "overflow"
-                        ? `Context overflow recovery failed: ${errorMessage}`
-                        : `Auto-compaction failed: ${errorMessage}`,
+                    errorMessage: aborted
+                        ? undefined
+                        : reason === "overflow"
+                            ? `Context overflow recovery failed: ${errorMessage}`
+                            : `Auto-compaction failed: ${errorMessage}`,
                 });
             }
             return false;
         }
         finally {
-            this._autoCompactionAbortController = undefined;
+            parentSignal?.removeEventListener("abort", abortFromParent);
+            // Only clear if we still own the field (another call may have taken over)
+            if (this._autoCompactionAbortController === localAbortController) {
+                this._autoCompactionAbortController = undefined;
+            }
+        }
+    }
+    async _resolveFreshContextResult(request) {
+        const handler = this._freshContextHandler;
+        if (typeof handler !== "function") {
+            return undefined;
+        }
+        const result = await handler(request);
+        if (result === undefined || result === null) {
+            return undefined;
+        }
+        if (!result || typeof result !== "object" || typeof result.summary !== "string") {
+            throw new Error("Fresh context handler returned an invalid result");
+        }
+        return result;
+    }
+    _materializeFreshContext(result) {
+        const summary = result.summary;
+        const tokensBefore = typeof result.tokensBefore === "number" && Number.isFinite(result.tokensBefore)
+            ? result.tokensBefore
+            : 0;
+        const details = result.details && typeof result.details === "object"
+            ? result.details
+            : { forgeContext: { mode: "fresh" } };
+        const firstKeptEntryId = this.sessionManager.appendCustomEntry("forge_context_boundary", {
+            mode: "fresh",
+        });
+        return { summary, firstKeptEntryId, tokensBefore, details };
+    }
+    _commitCompaction(summary, firstKeptEntryId, tokensBefore, details, fromExtension) {
+        const compactionId = this.sessionManager.appendCompaction(summary, firstKeptEntryId, tokensBefore, details, fromExtension);
+        const sessionContext = this.sessionManager.buildSessionContext();
+        this.agent.state.messages = sessionContext.messages;
+        const savedCompactionEntry = this.sessionManager.getEntry(compactionId);
+        return {
+            compactionId,
+            savedCompactionEntry: savedCompactionEntry?.type === "compaction" ? savedCompactionEntry : undefined,
+            estimatedTokensAfter: estimateMessagesTokens(sessionContext.messages),
+        };
+    }
+    async _notifyCompactionCommitted(options) {
+        if (!this._extensionRunner || !options.savedCompactionEntry) {
+            return;
+        }
+        try {
+            await this._extensionRunner.emit({
+                type: "session_compact",
+                compactionEntry: options.savedCompactionEntry,
+                fromExtension: options.fromExtension,
+                reason: options.reason,
+                willRetry: options.willRetry,
+            });
+        }
+        catch {
+            // Optional notification. A committed compaction ID is already durable success.
         }
     }
     /**
diff --git a/dist/core/auth-storage.d.ts b/dist/core/auth-storage.d.ts
index 12938fb01385a0e59a46698ae04ccf37c2d4f670..c26a12564dcd926f9ff2f4beb1b680deb180b6d1 100644
--- a/dist/core/auth-storage.d.ts
+++ b/dist/core/auth-storage.d.ts
@@ -68,6 +68,10 @@ export declare class AuthStorage {
      * Remove a runtime API key override.
      */
     removeRuntimeApiKey(provider: string): void;
+    /**
+     * Check whether a runtime API key override is currently effective.
+     */
+    hasRuntimeApiKey(provider: string): boolean;
     private recordError;
     private parseStorageData;
     /**
diff --git a/dist/core/auth-storage.js b/dist/core/auth-storage.js
index 2bec7263fdbb2d5ab96dae74b5ae430656641385..90383f317bcb208dede6ddeebcb1ba369db6e3da 100644
--- a/dist/core/auth-storage.js
+++ b/dist/core/auth-storage.js
@@ -178,6 +178,12 @@ export class AuthStorage {
     removeRuntimeApiKey(provider) {
         this.runtimeOverrides.delete(provider);
     }
+    /**
+     * Check whether a runtime API key override is currently effective.
+     */
+    hasRuntimeApiKey(provider) {
+        return Boolean(this.runtimeOverrides.get(provider));
+    }
     recordError(error) {
         const normalizedError = error instanceof Error ? error : new Error(String(error));
         this.errors.push(normalizedError);
diff --git a/dist/core/model-registry.js b/dist/core/model-registry.js
index 89ef507421d727f5cd4ad62c440b0f6bff68f3c0..06f3e08a2576416a55b15900f32889000b31ab68 100644
--- a/dist/core/model-registry.js
+++ b/dist/core/model-registry.js
@@ -135,6 +135,7 @@ const ModelDefinitionSchema = Type.Object({
     name: Type.Optional(Type.String({ minLength: 1 })),
     api: Type.Optional(Type.String({ minLength: 1 })),
     baseUrl: Type.Optional(Type.String({ minLength: 1 })),
+    authScope: Type.Optional(Type.Union([Type.Literal("any"), Type.Literal("oauth")])),
     reasoning: Type.Optional(Type.Boolean()),
     thinkingLevelMap: Type.Optional(ThinkingLevelMapSchema),
     input: Type.Optional(Type.Array(Type.Union([Type.Literal("text"), Type.Literal("image")]))),
@@ -262,6 +263,8 @@ export const clearApiKeyCache = clearConfigValueCache;
  */
 export class ModelRegistry {
     models = [];
+    modelsWithoutOAuth = [];
+    oauthRoutingModels = new WeakSet();
     providerRequestConfigs = new Map();
     modelRequestHeaders = new Map();
     configModelOverrides = new Map();
@@ -310,15 +313,59 @@ export class ModelRegistry {
             // Keep built-in models even if custom models failed to load
         }
         const builtInModels = this.loadBuiltInModels(overrides, modelOverrides);
-        let combined = this.mergeCustomModels(builtInModels, customModels);
-        // Let OAuth providers modify their models (e.g., update baseUrl)
-        for (const oauthProvider of this.authStorage.getOAuthProviders()) {
+        const combined = this.mergeCustomModels(builtInModels, customModels);
+        this.modelsWithoutOAuth = structuredClone(combined);
+        this.applyOAuthModelTransforms();
+    }
+    applyOAuthModelTransforms() {
+        this.models = structuredClone(this.modelsWithoutOAuth);
+        const oauthProviders = this.authStorage.getOAuthProviders();
+        // Let OAuth providers modify their models (e.g., update baseUrl), unless a
+        // higher-priority runtime API key is currently effective for that provider.
+        for (const oauthProvider of oauthProviders) {
             const cred = this.authStorage.get(oauthProvider.id);
-            if (cred?.type === "oauth" && oauthProvider.modifyModels) {
-                combined = oauthProvider.modifyModels(combined, cred);
+            if (!this.authStorage.hasRuntimeApiKey(oauthProvider.id) && cred?.type === "oauth" && oauthProvider.modifyModels) {
+                this.models = oauthProvider.modifyModels(this.models, cred);
             }
         }
-        this.models = combined;
+        const oauthRoutingProviderIds = new Set(oauthProviders.filter((provider) => provider.modifyModels).map((provider) => provider.id));
+        for (const model of this.models) {
+            if (oauthRoutingProviderIds.has(model.provider)) {
+                // Identity is the ownership boundary: arbitrary SDK model objects may
+                // intentionally reuse a registry provider/id with a different endpoint.
+                this.oauthRoutingModels.add(model);
+            }
+        }
+    }
+    isOAuthCredentialEffective(provider) {
+        return !this.authStorage.hasRuntimeApiKey(provider) && this.authStorage.get(provider)?.type === "oauth";
+    }
+    isModelAllowedByEffectiveCredential(model) {
+        return model.authScope !== "oauth" || this.isOAuthCredentialEffective(model.provider);
+    }
+    synchronizeModelForEffectiveCredential(model) {
+        if (!this.oauthRoutingModels.has(model)) {
+            return;
+        }
+        const baseline = this.modelsWithoutOAuth.find((candidate) => candidate.provider === model.provider && candidate.id === model.id);
+        if (!baseline) {
+            return;
+        }
+        let effective = baseline;
+        if (!this.authStorage.hasRuntimeApiKey(model.provider)) {
+            const cred = this.authStorage.get(model.provider);
+            const oauthProvider = this.authStorage.getOAuthProviders().find((candidate) => candidate.id === model.provider);
+            if (cred?.type === "oauth" && oauthProvider?.modifyModels) {
+                effective = oauthProvider.modifyModels([baseline], cred)[0] ?? baseline;
+            }
+        }
+        if (effective === model) {
+            return;
+        }
+        for (const key of Object.keys(model)) {
+            delete model[key];
+        }
+        Object.assign(model, structuredClone(effective));
     }
     /** Load built-in models and apply provider/model overrides */
     loadBuiltInModels(overrides, modelOverrides) {
@@ -485,6 +532,7 @@ export class ModelRegistry {
                     api: api,
                     provider: providerName,
                     baseUrl,
+                    authScope: modelDef.authScope,
                     reasoning: modelDef.reasoning ?? false,
                     thinkingLevelMap: modelDef.thinkingLevelMap,
                     input: (modelDef.input ?? ["text"]),
@@ -503,25 +551,28 @@ export class ModelRegistry {
      * If models.json had errors, returns only built-in models.
      */
     getAll() {
-        return this.models;
+        return this.models.filter((model) => this.isModelAllowedByEffectiveCredential(model));
     }
     /**
      * Get only models that have auth configured.
      * This is a fast check that doesn't refresh OAuth tokens.
      */
     getAvailable() {
-        return this.models.filter((m) => this.hasConfiguredAuth(m));
+        return this.models.filter((model) => this.isModelAllowedByEffectiveCredential(model) && this.hasConfiguredAuth(model));
     }
     /**
      * Find a model by provider and ID.
      */
     find(provider, modelId) {
-        return this.models.find((m) => m.provider === provider && m.id === modelId);
+        return this.models.find((model) => model.provider === provider && model.id === modelId && this.isModelAllowedByEffectiveCredential(model));
     }
     /**
      * Get API key for a model.
      */
     hasConfiguredAuth(model) {
+        if (!this.isModelAllowedByEffectiveCredential(model)) {
+            return false;
+        }
         const providerApiKey = this.providerRequestConfigs.get(model.provider)?.apiKey;
         return (this.authStorage.hasAuth(model.provider) ||
             (providerApiKey !== undefined && isConfigValueConfigured(providerApiKey)));
@@ -552,11 +603,22 @@ export class ModelRegistry {
      */
     async getApiKeyAndHeaders(model) {
         try {
+            // Routing and auth must be derived from the same effective credential. In
+            // particular, a runtime API key must undo an OAuth proxy model rewrite.
+            this.synchronizeModelForEffectiveCredential(model);
+            if (!this.isModelAllowedByEffectiveCredential(model)) {
+                return { ok: false, error: `Model "${model.provider}/${model.id}" requires OAuth authentication.` };
+            }
             const providerConfig = this.providerRequestConfigs.get(model.provider);
             const providerEnv = this.authStorage.getProviderEnv(model.provider);
-            const apiKeyFromAuthStorage = await this.authStorage.getApiKey(model.provider, { includeFallback: false });
+            // A persisted auth.json entry owns the provider credential slot. If an OAuth
+            // refresh fails, do not silently replace that credential with lower-priority auth.
+            const hasStoredCredential = this.authStorage.has(model.provider);
+            const apiKeyFromAuthStorage = await this.authStorage.getApiKey(model.provider, {
+                includeFallback: !hasStoredCredential,
+            });
             const apiKey = apiKeyFromAuthStorage ??
-                (providerConfig?.apiKey
+                (!hasStoredCredential && providerConfig?.apiKey
                     ? resolveConfigValueOrThrow(providerConfig.apiKey, `API key for provider "${model.provider}"`, providerEnv)
                     : undefined);
             const providerHeaders = resolveHeadersOrThrow(providerConfig?.headers, `provider "${model.provider}"`, providerEnv);
@@ -624,10 +686,16 @@ export class ModelRegistry {
      * Get API key for a provider.
      */
     async getApiKeyForProvider(provider) {
-        const apiKey = await this.authStorage.getApiKey(provider);
+        const hasStoredCredential = this.authStorage.has(provider);
+        const apiKey = await this.authStorage.getApiKey(provider, {
+            includeFallback: !hasStoredCredential,
+        });
         if (apiKey !== undefined) {
             return apiKey;
         }
+        if (hasStoredCredential) {
+            return undefined;
+        }
         const providerApiKey = this.providerRequestConfigs.get(provider)?.apiKey;
         return providerApiKey
             ? resolveConfigValueUncached(providerApiKey, this.authStorage.getProviderEnv(provider))
@@ -638,7 +706,7 @@ export class ModelRegistry {
      */
     isUsingOAuth(model) {
         const cred = this.authStorage.get(model.provider);
-        return cred?.type === "oauth";
+        return !this.authStorage.hasRuntimeApiKey(model.provider) && cred?.type === "oauth";
     }
     /**
      * Register a provider dynamically (from extensions).
@@ -725,8 +793,10 @@ export class ModelRegistry {
         }
         this.storeProviderRequestConfig(providerName, config);
         if (config.models && config.models.length > 0) {
-            // Full replacement: remove existing models for this provider
-            this.models = this.models.filter((m) => m.provider !== providerName);
+            // Full replacement: mutate the non-OAuth baseline, then derive effective
+            // routing from it so request-time credential synchronization cannot restore
+            // stale built-in endpoints over dynamically registered models.
+            this.modelsWithoutOAuth = this.modelsWithoutOAuth.filter((m) => m.provider !== providerName);
             // Parse and add new models
             for (const modelDef of config.models) {
                 const api = modelDef.api || config.api;
@@ -741,6 +811,7 @@ export class ModelRegistry {
                     api: api,
                     provider: providerName,
                     baseUrl: modelDef.baseUrl ?? config.baseUrl,
+                    authScope: modelDef.authScope,
                     reasoning: modelDef.reasoning,
                     thinkingLevelMap: modelDef.thinkingLevelMap,
                     input: modelDef.input,
@@ -750,19 +821,12 @@ export class ModelRegistry {
                     headers: undefined,
                     compat: modelDef.compat,
                 });
-                this.models.push(model);
-            }
-            // Apply OAuth modifyModels if credentials exist (e.g., to update baseUrl)
-            if (config.oauth?.modifyModels) {
-                const cred = this.authStorage.get(providerName);
-                if (cred?.type === "oauth") {
-                    this.models = config.oauth.modifyModels(this.models, cred);
-                }
+                this.modelsWithoutOAuth.push(model);
             }
         }
         else if (config.baseUrl || config.headers) {
-            // Override-only: update baseUrl for existing models. Request headers are resolved per request.
-            this.models = this.models.map((m) => {
+            // Override-only: update the non-OAuth baseline. Request headers are resolved per request.
+            this.modelsWithoutOAuth = this.modelsWithoutOAuth.map((m) => {
                 if (m.provider !== providerName)
                     return m;
                 return {
@@ -771,6 +835,9 @@ export class ModelRegistry {
                 };
             });
         }
+        // Rebuild from the canonical + models.json + dynamic-provider baseline before
+        // applying OAuth transforms. Endpoint routing and provider keys then stay paired.
+        this.applyOAuthModelTransforms();
     }
 }
 //# sourceMappingURL=model-registry.js.map
\ No newline at end of file
diff --git a/dist/core/session-manager.js b/dist/core/session-manager.js
index 249d1068f95d389504a1c0a78cc316d8d886f085..8b336285ea8ec25295131b182aa06118f868bb03 100644
--- a/dist/core/session-manager.js
+++ b/dist/core/session-manager.js
@@ -664,7 +664,8 @@ export class SessionManager {
         if (!this.persist || !this.sessionFile)
             return;
         const hasAssistant = this.fileEntries.some((e) => e.type === "message" && e.message.role === "assistant");
-        if (!hasAssistant) {
+        const isFreshCheckpoint = entry?.type === "compaction" && entry.details?.forgeContext?.mode === "fresh";
+        if (!hasAssistant && !isFreshCheckpoint) {
             if (this.flushed) {
                 appendFileSync(this.sessionFile, `${JSON.stringify(entry)}\n`);
             }
