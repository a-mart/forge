{
  "piSessionFormatVersion": 3,
  "forgeBaseline": "0.80.6",
  "forgeCommit": "wp-8-current",
  "fixtures": [
    {
      "id": "compat-matrix",
      "file": "compat-matrix.jsonl",
      "description": "Full v3 session matrix: user/assistant/tool/custom/custom_message/model/thinking/branch/compaction/labels."
    },
    {
      "id": "aborted-stream-tail",
      "file": "aborted-stream-tail.jsonl",
      "description": "Assistant stream aborted after a partial assistant message; file remains valid JSONL."
    },
    {
      "id": "interrupted-tool-call",
      "file": "interrupted-tool-call.jsonl",
      "description": "Assistant tool call started but no toolResult was persisted; open must not replay or execute tools."
    },
    {
      "id": "truncated-tail",
      "file": "truncated-tail.jsonl",
      "description": "Crash-truncated malformed tail after a valid prefix; readers must recover prefix and append safely via Forge timeline."
    },
    {
      "id": "crash-during-compaction",
      "file": "crash-during-compaction.jsonl",
      "description": "Compaction start marker without completed compaction summary; old messages remain authoritative."
    }
  ],
  "rollbackPolicy": "0.80.6-written v3 state opens under the frozen 0.71.1 runner for this fixture matrix. If a future format does not, rollback is snapshot-restore-only from a pre-upgrade data copy; do not downgrade in-place."
}
